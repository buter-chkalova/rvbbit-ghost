name: ci

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  powershell-static-tests:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run static and behavior tests
        shell: pwsh
        run: ./scripts/Test-RvbbitGhost.ps1

